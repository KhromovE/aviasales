import styled from 'styled-components'

export const TicketListTemplate = styled.div`
  display: grid;
  justify-content: center;
  grid-template-columns: 1fr;
  grid-auto-rows: auto;
  grid-gap: var(--spacing-md);
`
