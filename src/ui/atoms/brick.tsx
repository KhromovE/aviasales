import styled from 'styled-components'

export const Brick = styled.section`
  padding: var(--spacing-md) 0;
  background: var(--white);
  box-shadow: 0px 2px 8px var(--shadow);
  border-radius: var(--border-radius-sm);
`
